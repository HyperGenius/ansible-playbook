# ユーザーによって入力される変数のテスト用ファイル
# アップロードするジョブ結果のファイル名, ホスト単位でディレクトリを作成
job_result_filepath: "{{ __workflowdir__ }}/{{ inventory_hostname }}/sample_job_result.json"
upload_log_filepath: "{{ __workflowdir__ }}/{{ inventory_hostname }}/sample_job_result.log"
